def load_mnist():
    
    path = r'C:\Users\Administrator\Desktop\study\mnist.npz' #放置mnist.py的目录。注意斜杠
    f = np.load(path)
    x_train, y_train = f['x_train'], f['y_train']
    x_test, y_test = f['x_test'], f['y_test']
    f.close()
    return (x_train, y_train), (x_test, y_test)
 
(train_image,train_label),(test_image,test_label) = load_mnist()



登录官方网站 http://yann.lecun.com/exdb/mnist/手动下载

或直接由python下载：(x_train, y_train), (x_test, y_test) = mnist.load_data()
